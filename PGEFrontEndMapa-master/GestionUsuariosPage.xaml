<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IntegrarMapa.GestionUsuariosPage"
             Title="Gestión de usuarios">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- 🔙 Botón volver -->
            <Button Text="⬅ Volver"
                    BackgroundColor="#E53935"
                    TextColor="White"
                    CornerRadius="10"
                    WidthRequest="100"
                    Clicked="OnVolverClicked" />

            <!-- 🔍 Búsqueda -->
            <Label Text="Buscar usuario"
                   FontAttributes="Bold"
                   FontSize="20"
                   HorizontalOptions="Center" />

            <Picker x:Name="pickerCriterio"
                    Title="Buscar por..."
                    ItemsSource="{Binding CriteriosBusqueda}" />

            <Entry x:Name="entryBusqueda"
                   Placeholder="Ingrese nombre, email o usuario" />

            <Button Text="Buscar"
                    BackgroundColor="#333333"
                    TextColor="White"
                    CornerRadius="8"
                    Clicked="OnBuscarClicked" />

            <!-- 🧾 Gestión -->
            <Button Text="Gestión de usuario"
                    BackgroundColor="Transparent"
                    TextColor="#E53935"
                    BorderColor="#E53935"
                    BorderWidth="1"
                    CornerRadius="8"
                    HorizontalOptions="Center" />

            <Label Text="Nombre y apellido:" FontAttributes="Bold"/>
            <Label x:Name="lblNombre" Text="-----" />

            <Label Text="Email:" FontAttributes="Bold"/>
            <Label x:Name="lblEmail" Text="-----" />

            <Label Text="Teléfono:" FontAttributes="Bold"/>
            <Label x:Name="lblTelefono" Text="-----" />

            <Label Text="Usuario:" FontAttributes="Bold"/>
            <Label x:Name="lblUsuario" Text="-----" />

            <!-- Icono usuario -->
            <Image Source="user_icon.png"
                   HeightRequest="100"
                   WidthRequest="100"
                   HorizontalOptions="Center" />

            <!-- Rol -->
            <Label Text="Rol" FontAttributes="Bold"/>
            <Picker x:Name="pickerRol">
                <Picker.Items>
                    <x:String>Cliente</x:String>
                    <x:String>Operador</x:String>
                </Picker.Items>
            </Picker>

            <!-- Botones de acción -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,10,0,0">
                <Button Text="Guardar cambios"
                        Grid.Column="0"
                        BorderColor="#E53935"
                        BorderWidth="1"
                        BackgroundColor="Transparent"
                        TextColor="#E53935"
                        CornerRadius="8"
                        Clicked="OnGuardarClicked"/>

                <Button Text="Eliminar usuario"
                        Grid.Column="1"
                        BorderColor="#E53935"
                        BorderWidth="1"
                        BackgroundColor="Transparent"
                        TextColor="#E53935"
                        CornerRadius="8"
                        Clicked="OnEliminarClicked"/>
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
